// Database configuration file
// This connects to Prisma for your courses, users, and products

export const databaseConfig = {
  isConnected: false
}

// Simple database utility for now
// We'll add Prisma later when we set up the database
